import streamlit as st
import random

st.set_page_config(page_title="3分学習メーカー", page_icon="⚡")

st.title("⚡ 3分学習メーカー")
st.write("今日は“3分だけ”進もう。気軽に小さな一歩を作ろう！")

# タスクデータ
tasks = {
    "Python": [
        "print文を1行だけ書く",
        "リストを3つ作るだけ",
        "既存コードのコメントを1行だけ追加する",
        "Streamlitでタイトルを1行変更する",
        "Pythonの本を1ページ読む"
    ],
    "Unity": [
        "Cubeを1個置くだけ",
        "ライトを1つ置く",
        "プレハブをひとつ作る",
        "テキストを1行表示する",
        "Button→Debug.Log を設定する"
    ],
    "3D": [
        "立方体を1つ追加する",
        "頭の角度を1mmだけ変える",
        "ビューを変えて保存する",
        "UV展開を少しだけ触る",
        "マテリアルを1つ適用"
    ],
    "画像生成": [
        "同じ構図で2枚だけ生成する",
        "色調を変えたバージョンを1枚作る",
        "プロンプトを5文字調整する",
        "構図だけ変更する",
        "背景のみ差し替える"
    ],
    "株": [
        "1銘柄だけ検索する",
        "チャートを1つ見るだけ",
        "今日のニュースを2個読む",
        "日経平均だけ確認する",
        "気になった企業をメモする"
    ],
    "Note投稿": [
        "タイトル案を3つ出す",
        "導入文を1行だけ書く",
        "タグ候補を3つ考える",
        "今日の気付きをメモする",
        "今週話題にしたいテーマを考える"
    ],
}

# UI
category = st.selectbox("学習ジャンルを選んでね", list(tasks.keys()))

if st.button("3分タスクを生成！"):
    st.subheader("今日の小さな一歩")
    picks = random.sample(tasks[category], 3)
    for i, p in enumerate(picks, 1):
        st.write(f"**{i}. {p}**")
